# 前端综合面试题
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. 性能优化

```
Q: 首屏加载优化方案？
A:
- 路由懒加载 + 代码分割
- 关键 CSS 内联
- 资源预加载（preload/prefetch）
- SSR/SSG
- 骨架屏
- 图片懒加载 + WebP 格式
- Gzip/Brotli 压缩
- CDN 加速
- Tree Shaking
- 减少第三方库体积（按需导入）

Q: 如何定位性能瓶颈？
A:
- Lighthouse 评分
- Chrome DevTools Performance 面板
- Web Vitals 指标监控
- Network 面板分析资源加载
- React/Vue DevTools Profiler
```

## 2. 浏览器原理

```
Q: 从输入 URL 到页面显示的过程？
A:
1. DNS 解析（域名 → IP）
2. TCP 三次握手（+ TLS 握手）
3. 发送 HTTP 请求
4. 服务器处理请求，返回响应
5. 浏览器解析 HTML → DOM 树
6. 解析 CSS → CSSOM 树
7. 合并 → 渲染树
8. 布局（Layout）→ 绘制（Paint）→ 合成（Composite）
9. 执行 JavaScript

Q: 跨域解决方案？
A:
- CORS（服务端设置 Access-Control-Allow-Origin）
- 代理服务器（开发环境 Vite/Webpack proxy，生产环境 Nginx）
- JSONP（仅 GET，已过时）
- WebSocket（不受同源策略限制）
```

## 3. 工程化

```
Q: Webpack 和 Vite 的区别？
A:
- Webpack：打包所有模块后启动，HMR 需要重新构建
- Vite：利用浏览器原生 ESM，按需编译，启动极快
- Vite 开发体验更好，Webpack 生态更成熟
- Vite 生产环境用 Rollup 打包

Q: Tree Shaking 原理？
A:
- 基于 ES Module 的静态分析
- 标记未使用的导出（unused exports）
- 在压缩阶段移除未使用的代码
- 要求使用 import/export，不能用 require
- package.json 中 sideEffects 标记副作用文件
```

## 4. 设计模式与架构

```
Q: 前端常用设计模式？
A:
- 观察者/发布订阅（事件系统、状态管理）
- 策略模式（表单验证、权限控制）
- 代理模式（Proxy 响应式、图片懒加载）
- 装饰器模式（HOC、中间件）
- 单例模式（全局状态、弹窗管理）
- 工厂模式（组件创建）

Q: 微前端的应用场景？
A:
- 大型应用拆分为独立子应用
- 多团队并行开发
- 渐进式技术栈迁移
- 遗留系统集成
```

## 5. 算法题（高频）

```javascript
// 两数之和
function twoSum(nums, target) {
  const map = new Map();
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) return [map.get(complement), i];
    map.set(nums[i], i);
  }
}

// 有效括号
function isValid(s) {
  const stack = [];
  const map = { ')': '(', ']': '[', '}': '{' };
  for (const c of s) {
    if ('([{'.includes(c)) stack.push(c);
    else if (stack.pop() !== map[c]) return false;
  }
  return stack.length === 0;
}

// 反转链表
function reverseList(head) {
  let prev = null, curr = head;
  while (curr) {
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev;
}

// 最大子数组和（Kadane）
function maxSubArray(nums) {
  let maxSum = nums[0], currentSum = nums[0];
  for (let i = 1; i < nums.length; i++) {
    currentSum = Math.max(nums[i], currentSum + nums[i]);
    maxSum = Math.max(maxSum, currentSum);
  }
  return maxSum;
}

// LRU 缓存
class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.cache = new Map();
  }
  get(key) {
    if (!this.cache.has(key)) return -1;
    const value = this.cache.get(key);
    this.cache.delete(key);
    this.cache.set(key, value);
    return value;
  }
  put(key, value) {
    if (this.cache.has(key)) this.cache.delete(key);
    else if (this.cache.size >= this.capacity) {
      this.cache.delete(this.cache.keys().next().value);
    }
    this.cache.set(key, value);
  }
}
```
