# 架构与性能面试题
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. MVC vs MVVM

```swift
// MVC 问题：Massive View Controller
// Controller 承担：网络请求、数据处理、UI 更新、事件响应
// 导致：难以测试、代码臃肿、职责不清

// MVVM 优势：
// - ViewModel 可独立测试（不依赖 UIKit）
// - 数据绑定减少胶水代码
// - 职责清晰：View 只负责展示

class UserViewModel: ObservableObject {
    @Published var displayName = ""
    @Published var isLoading = false

    private let useCase: FetchUserUseCase

    init(useCase: FetchUserUseCase) { self.useCase = useCase }

    // 可测试的业务逻辑
    @MainActor
    func load(userId: Int) async {
        isLoading = true
        defer { isLoading = false }
        if let user = try? await useCase.execute(id: userId) {
            displayName = "\(user.name) (\(user.email))"
        }
    }
}

// 测试 ViewModel（无需 UI）
func testLoadUser() async {
    let mockUseCase = MockFetchUserUseCase()
    let vm = UserViewModel(useCase: mockUseCase)
    await vm.load(userId: 1)
    XCTAssertEqual(vm.displayName, "张三 (zhang@test.com)")
}
```

## 2. 启动优化策略

```swift
// 冷启动优化清单：
// Pre-main 阶段：
// 1. 减少动态库（合并为静态库）
// 2. 减少 ObjC 类数量
// 3. 避免 +load 方法

// Post-main 阶段：
// 1. didFinishLaunching 只做必要初始化
// 2. 延迟非首屏依赖的初始化
// 3. 首屏数据预加载 + 缓存

class AppLauncher {
    func launch() {
        // 阶段1：同步必要初始化
        setupCrashReporter()
        setupRootWindow()

        // 阶段2：首帧渲染后异步初始化
        DispatchQueue.main.async {
            self.setupAnalytics()
            self.setupPush()
            self.prefetchData()
        }

        // 阶段3：后台低优先级初始化
        DispatchQueue.global(qos: .utility).async {
            self.warmUpDatabase()
            self.cleanExpiredCache()
        }
    }
}
```

## 3. 内存泄漏检测

```swift
// 常见泄漏场景及解决方案

// 1. 闭包循环引用
class ViewModel {
    var onComplete: (() -> Void)?
    // ❌ onComplete = { self.doSomething() }
    // ✅ onComplete = { [weak self] in self?.doSomething() }
}

// 2. Delegate 循环引用
protocol ServiceDelegate: AnyObject { }
class Service {
    weak var delegate: ServiceDelegate?  // 必须 weak
}

// 3. Timer 循环引用
// ❌ Timer.scheduledTimer(target: self, ...)
// ✅ Timer.scheduledTimer(withTimeInterval:repeats:block:) + [weak self]

// 4. 检测工具
// - Xcode Memory Graph Debugger
// - Instruments Leaks
// - deinit 打印验证
deinit { print("\(type(of: self)) 已释放") }

// 5. MLeaksFinder（第三方检测）
// 页面 pop/dismiss 后检测对象是否释放
```

## 4. 列表性能优化

```swift
// 核心优化点：
// 1. Cell 复用 + prepareForReuse 重置状态
// 2. 避免离屏渲染（cornerRadius + clipsToBounds）
// 3. 异步图片加载 + 降采样
// 4. 高度缓存（避免重复计算）
// 5. 预加载（prefetchRowsAt）

class OptimizedCell: UITableViewCell {
    override func prepareForReuse() {
        super.prepareForReuse()
        imageView?.image = nil
        currentTask?.cancel()
    }

    func configure(with item: Item) {
        // 文本直接赋值（主线程）
        titleLabel.text = item.title

        // 图片异步加载 + 降采样
        currentTask = Task {
            let image = await ImageLoader.shared.load(
                url: item.imageURL,
                targetSize: CGSize(width: 60, height: 60)
            )
            guard !Task.isCancelled else { return }
            avatarView.image = image
        }
    }
}
```

## 5. 网络层设计

```swift
// 面试常问：如何设计一个可测试、可扩展的网络层

protocol APIEndpoint {
    var path: String { get }
    var method: HTTPMethod { get }
    var headers: [String: String] { get }
    associatedtype Response: Decodable
}

struct GetUsersEndpoint: APIEndpoint {
    typealias Response = [User]
    var path = "/users"
    var method: HTTPMethod = .get
    var headers: [String: String] = [:]
}

protocol NetworkClient {
    func send<E: APIEndpoint>(_ endpoint: E) async throws -> E.Response
}

// 真实实现
class URLSessionNetworkClient: NetworkClient {
    func send<E: APIEndpoint>(_ endpoint: E) async throws -> E.Response {
        let (data, _) = try await URLSession.shared.data(for: endpoint.urlRequest)
        return try JSONDecoder().decode(E.Response.self, from: data)
    }
}

// Mock 实现（测试用）
class MockNetworkClient: NetworkClient {
    var mockData: Data?
    func send<E: APIEndpoint>(_ endpoint: E) async throws -> E.Response {
        return try JSONDecoder().decode(E.Response.self, from: mockData!)
    }
}
```

## 6. 包体积与编译优化

```swift
// 包体积优化：
// 1. Asset Catalog 管理资源（自动 App Slicing）
// 2. 移除未使用代码和资源
// 3. 动态库转静态库
// 4. 图片使用 WebP / HEIF 格式
// 5. On-Demand Resources 按需下载

// 编译优化：
// 1. 模块化（SPM 分包，增量编译）
// 2. 减少复杂类型推断
// 3. 使用 Xcode Build Timeline 分析瓶颈
// 4. 开启 Build Active Architecture Only（Debug）
```
